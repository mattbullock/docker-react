sudo: required
services:
  - docker

before_install:
  - docker build -t mattbullock/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mattbullock/docker-react npm run test

language: generic
